[project]
name = "nlp-medical-dataset"
version = "0.1.0"
description = "NLP medical dataset generation with CrewAI and local Ollama Mistral"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
